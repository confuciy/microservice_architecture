{{- if .Values.rabbitmqConsumer.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rabbitmq-notification-consumer
spec:
  replicas: {{ .Values.rabbitmqConsumer.replicas }}
  selector:
    matchLabels:
      app: rabbitmq-notification-consumer
  template:
    metadata:
      labels:
        app: rabbitmq-notification-consumer
    spec:
      containers:
      - name: consumer
        image: "{{ .Values.image.repository }}:{{ .Values.image.tags.service_notification }}"
        imagePullPolicy: Always
        envFrom:
        - secretRef:
            name: {{ .Release.Name }}-secrets
        command: ["php", "-r"] # используем sh для выполнения нескольких команд
        args:
          - "require './src/Service/Notification/RabbitMQ/NotificationReceive.php';"
        resources:
          {{- toYaml .Values.rabbitmqConsumer.resources | nindent 10 }}
{{- end }}