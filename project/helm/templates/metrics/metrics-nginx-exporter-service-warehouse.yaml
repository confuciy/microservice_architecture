apiVersion: v1
kind: Service
metadata:
  name: metrics-nginx-exporter-service-warehouse
  labels:
    app: metrics-nginx-exporter-service-warehouse
spec:
  selector:
    app: service-warehouse  # Должен совпадать с лейблами пода
  ports:
    - protocol: TCP
      port: 9113  # Порт сервиса
      targetPort: 9113  # Порт пода, где слушает экспортер