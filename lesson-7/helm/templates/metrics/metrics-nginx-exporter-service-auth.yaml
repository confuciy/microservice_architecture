apiVersion: v1
kind: Service
metadata:
  name: metrics-nginx-exporter-service-auth
  labels:
    app: metrics-nginx-exporter-service-auth
spec:
  selector:
    app: service-auth  # Должен совпадать с лейблами пода
  ports:
    - protocol: TCP
      port: 9113  # Порт сервиса
      targetPort: 9113  # Порт пода, где слушает экспортер